export type paymentDetail = {
    _id: string,
    cardNumber: string,
    expiryMonth: string,
    expiryYear: string,
    cvv: string,
    address: string,
    cardholderName: string,
    credit: number,
    createdAt: string
}