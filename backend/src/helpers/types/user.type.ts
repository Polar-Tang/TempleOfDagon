type userType = {
    name: String,
    password: String,
    email:  String,
    emailVerified:  Boolean,
    verificationToken: String,
    timeStamp: Date,
    active: Boolean,
    role: String,
}

export default userType